import 'package:flutter/material.dart';

const primaryColor = Colors.purple;
const secondaryHeaderColor = Colors.blue;
const Color primaryBackground = primaryColor;
const Color flightColor = primaryColor;
final Color chipBackground = secondaryHeaderColor.withOpacity(.2);
final Color borderColor = primaryColor.withAlpha(100);
